# MongoDB credentials stored as a Kubernetes Secret
# Values must be base64-encoded.
# To encode:  echo -n 'yourvalue' | base64
# To decode:  echo 'encodedvalue' | base64 -d
#
# Current values:  username=admin  password=qwerty
apiVersion: v1
kind: Secret
metadata:
  name: mongo-secret
  namespace: testapp-ns
type: Opaque
data:
  username: YWRtaW4=      # admin
  password: cXdlcnR5    # qwerty
