{
  "name": "docker-testapp",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "test": "jest --testPathPattern=tests/ --forceExit --coverage",
    "lint": "eslint . --ext .js --ignore-path .eslintignore"
  },
  "dependencies": {
    "express": "^4.21.2",
    "mongodb": "^6.13.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
